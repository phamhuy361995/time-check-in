{
  "name": "time-check-in-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node --watch local.js",
    "start": "node local.js",
    "check": "node --check server.js && node --check app.js && node --check db.js && node --check payroll.js && node --check local.js"
  },
  "dependencies": {
    "@vercel/functions": "^3.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "pg": "^8.13.1"
  }
}
